<aside>
💡

로그인 페이지 구성 및 각 요소들의 이벤트 처리 방법을 정의 한다.

</aside>

## UI Style

- "./2. UI STYLE" 참고
- UI 구성 요소 정렬
  - center

- DropdownInput
  - Dropdown & Input box 기능이 혼합 된 형태 이다.
  - DropdownInput 컴포넌트를 사용하여 원하는 값을 선택하거나 텍스트를 입력하여 검색이 가능 하다.

- UI 구성 요소 중 사용자 입력 필수
  - required가 true면 사용자 입력이 필수 이다.
  - 페이지 UI Section에 노출되는 이름 옆에 "*" 를 표시하고 "*" 색상은 red 계열로 하여 강조 한다.
  
## UI 구성

- Service 선택 영역
  - label: Service
  - required: true
  - DropdownInput
    - placeholder: "선택해 주세요"
    - values:
      - src/lib/types/wpayServerType.ts 의 SERVICE_OPTIONS 값.
      - 중복 선택 불가.
    - default value:
      - 브라우저 localStorage에 "service" key 값이 있으면 localStorage의 "service" key 값으로 초기화 한다.

- Server 선택 영역
  - label: Server
  - required: true
  - Radio Button
    - values:
      - src/lib/types/wpayServerType.ts 의 SERVER_TYPES 값
      - 중복 선택 불가.
    - default value:
      - 빈 값으로 한다.
      - 브라우저 localStorage에 "server" key 값이 있으면 localStorage의 "server" key 값으로 초기화 한다.
    - UI
      - 모양:
        - 모서리 둥근 네모 모양
        - 텍스트가 버튼 안에 표시되는 버튼과 같은 모양으로 표시한다.
      - 테두리
        - 색상: oklch(0.36 0.11 265.06)
        - 굵기: 중간
        - 모양: 둥근 모양

- PROD-Domain 선택 Modal
  - label: PROD-Domain
  - required: true
  - Server 선택 영역 Radio Button 값이 PROD로 선택 했을 때 PROD Domain 선택을 위해 Modal을 오픈한다.
  - Radio Button
    - values:
      - src/lib/types/wpayServerType.ts 의 PROD_SERVER_DOMAINS 값을 Radio Button 세로 정렬로 표시한다.
      - PROD_SERVER_DOMAINS 값을 선택 하면 label: Server* 문구 오른쪽에 "(선택한 PROD_SERVER_DOMAINS)" 문구를 표시 한다.
      - Server 선택 영역 Radio Button 값이 STG 또는 DEV로 변경 되면 Radio Button PROD 텍스트 오른쪽에 삽입된 문구는 제거 한다.
      - 중복 선택 불가.
    - default value:
      - 빈 값으로 한다.
      - 브라우저 localStorage에 "prodDomain" key 값이 있으면 localStorage의 "prodDomain" key 값으로 초기화 한다.
        - label: Server* 문구 오른쪽에 ("prodDomain" key의 값) 문구를 표시한다.
    - UI
      - 모양:
        - 모서리 둥근 네모 모양
        - 텍스트가 버튼 안에 표시되는 버튼과 같은 모양으로 표시한다.
      - 테두리
        - 색상: oklch(0.36 0.11 265.06)
        - 굵기: 중간
        - 모양: 둥근 모양
      - 사이즈:
        - width: 100%

- Site 선택 영역.
  - label: Site
  - required: true
  - DropdownInput
    - placeholder: "선택해 주세요"
    - values:
      - src/lib/types/wpayServerType.ts 의 SITE_OPTIONS 값.
      - 중복 선택 불가.
    - default value:
      - 브라우저 localStorage에 "site" key 값이 있으면 localStorage의 "site" key 값으로 초기화 한다.

- Merchant-ID 선택 영역.
  - label: Merchant-ID
  - required: true
  - DropdownInput
    - placeholder: "선택해 주세요"
    - values:
      - src/lib/types/wpayServerType.ts 의 MERCHANT_ID_OPTIONS 값.
      - 중복 선택 불가.
    - default value:
      - 브라우저 localStorage에 "mid" key 값이 있으면 localStorage의 "mid" key 값으로 초기화한다.

- Member-ID 입력 영역.
  - label: Member-ID
  - required: false
  - text Input
    - placeholder: "wpayTestUser01"
    - default value:
      - 빈 값으로 한다.
      - 브라우저 localStorage에 "userId" key 값이 있으면 localStorage의 "userId" key 값으로 초기화 한다.

- Cell-Phone-Number 입력 영역.
  - label: Cell-Phone-Number
  - required: false
  - number Input
    - placeholder: "입력해 주세요."
    - 복사한 문자열을 붙여 넣기 할 경우 숫자를 제외한 문자를 제거한다.
    - default value:
      - 빈 값으로한다.
      - 브라우저 localStorage에 "hNum" key 값이 있으면 localStorage의 "hNum" key 값으로 초기화 한다.

- isSaveCache Check Box 영역.
  - label: isSaveCache
  - required: false
  - text: "Should I save to cache?"
    - 체크박스 오른쪽에 문구를 표시한다.
  - Check Box
    - default value:
      - 체크 박스를 체크하지 않는다.
      - 브라우저의 localStorage에 "isSaveCache" Key 값이 있으면 localStorage의 "isSaveCache" Key 값으로 초기화 한다.
        - true: 체크 박스를 체크 한다.
        - false: 체크 박스를 체크하지 않는다.

- Next Button 영역.
  - label: nextButton
  - text: Next
  - 버튼 활성화 조건
    - label: service, server, site, mid 값이 모두 선택 되었을 때
    - click 이벤트 처리
      - "./3.1 서비스 가입.md" 또는 "./3.2 로그인 STEP01.md"을 조건에 따라 선택하여 이동한다.
      - localStorage 입력 정보 저장. (nextButton이 클릭되면 일단 localStorage에 저장하자)
        - localStorage key: service, value: label:Service 값
        - localStorage key: server, value: label:Server 값
        - localStorage key: prodDomain, value: label:PROD-Domain 값
        - localStorage key: site, value: label:Site 값
        - localStorage key: mid, value: label:Merchant-ID 값
        - localStorage key: userid, value: label:Member-ID 값 (입력 값이 빈값이면 "wpayTestUser01" 값을 강제로 세팅 한다.)
        - localStorage key: hNum, value: label:Cell-Phone-Number 값
        - localStorage key: isSaveCache, value: label:isSaveCache 체크 박스 값 (true/false)
          - 메인페이지에서 localStorage key "isSaveCache" 값이 true가 아니면 위 정보들을 localStorage에서 모두 삭제 한다.
  - 버튼 비활성화 조건
    - label: Service, Server, Site, mMerchant-ID 중 선택 되지 않은 값이 있을 때.
    - Mouse Over
      - 입력 또는 선택이 필요한 영역을 표시 한다.
      - radio button 의 경우 radio button 의 테두리 색상을 변경 한다.
      - 마우스가 Next Button 영역을 벗어나면 원래 스타일로 돌아온다.
    - Touch (모바일 환경)
      - 입력 또는 선택이 필요한 영역을 표시 한다.
      - radio button 의 경우 radio button 의 테두리 색상을 변경 한다.
      - 2초 뒤 원래 스타일로 서서히(2초동안) 돌아온다.
