---
description: github에 변경 사항 업로드
---

// turbo-all

github에 변경 사항 업로드 한다.

1. **github에 변경 사항 업로드 한다.**
   "git add -A" 명령어를 실행 한다.
   "git commit -m {commit message}" 명령어를 실행 한다.
   - commit message는 작업한 내용을 간략하게 기재한다.
   "git push" 명령어를 실행 한다.